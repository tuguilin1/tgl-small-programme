<view class="foods">
	<view>
		<cook bindtap="jumpDetail" data-id="{{item.id}}" wx:key="{{index}}" wx:for="{{list}}" name="{{item.name}}" id="{{item.id}}" preparetime="{{item.preparetime}}" pic="{{item.pic}}"></cook>
	</view>
	
</view>